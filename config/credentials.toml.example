# Copy to credentials.toml, fill in your values, then: chmod 600 credentials.toml
# credentials.toml is gitignored — never commit real values.
# Env vars POLYMARKET_PK / POLYMARKET_FUNDER override this file.

[credentials]
private_key = ""
funder = ""
